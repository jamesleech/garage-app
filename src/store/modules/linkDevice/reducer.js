import { linkDevice } from './index';

const initialState = {
  loading: false,
};

export const reducer = (state = initialState, action) => 
  // switch (action.type) {
  //   case linkDevice.SUCCESS:
  //     return {
  //       ...state,
  //     };
  //   default:
  //     return state
  // }
   state
;
